"""Authored scene graph specs (design: visual pipeline).

Each module in this package exposes a ``SPECS`` list of ``SceneGraphSpec``
objects. They declare the named nodes and adjacency that a kind of
location can have; the engine handles instantiation, generation,
prefetch, and adoption.
"""
